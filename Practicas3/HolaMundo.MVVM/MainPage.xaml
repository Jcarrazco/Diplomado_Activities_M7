<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HolaMundo.MVVM.MainPage"
             xmlns:vm="clr-namespace:HolaMundo.MVVM.ViewModels" >
    <ContentPage.BindingContext>
        <vm:ClienteViewModel />
    </ContentPage.BindingContext>
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label Text="Nombre"/>
            <Entry Text="{Binding Cliente.Nombre}"/>

            <Label Text="Apellidos"/>
            <Entry Text="{Binding Cliente.Apellidos}"/>

            <Button Text="Click me" 
                    Command="{Binding Command}"/>

            <Label Text="{Binding Mensaje}" Style="{StaticResource SubHeadline}"/>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
